import React, { useEffect } from 'react';
import Nav from "../frontend/src/tools/Nav";



const Home = ({user}) => {
  useEffect(() => {
    if (user?.isLoggedIn) {
      // display something if the user is logged in, hide it otherwise.
    }
  }, [user, session]);


  return (






  )


}